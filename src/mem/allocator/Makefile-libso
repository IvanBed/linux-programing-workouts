TARGET = myalloc_lib.so
 
all: $(TARGET)

myalloc_lib.so: alloc.o alloc_utils.o
	gcc -shared alloc.o alloc_utils.o -o $(TARGET)

alloc.o: alloc.c
	gcc -c -fPIC alloc.c -o alloc.o

alloc_utils.o: alloc_utils.c
	gcc -c -fPIC alloc_utils.c -o alloc_utils.o

clean:
	rm -rf *.o myalloc
